# Holocron install script — Windows (PowerShell)
#
# NOT YET UPDATED for spec 001 (unified config store, Claude Code + pi).
# This script still reflects the retired OpenCode-only layout and does not
# implement the converge/--check contract (contracts/install-cli.md). Run
# install.sh on macOS/Linux instead. See DECISIONS.md 2026-08-28 "Retire
# OpenCode harness support" and spec 001 research.md R7.

Write-Host "install.ps1 is not yet updated for spec 001 — run install.sh on macOS/Linux instead."
exit 0
