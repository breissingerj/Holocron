## Critical Rules — Claude Code Only

- **Session naming (CRITICAL)** — At the start of EVERY response where the task is clear, immediately run `/rename <label>` with a short slug describing the active work (e.g., `fix-auth-bug`, `lahzo-pixel-tracking`, `opencode-research`). Update the name if the task pivots. Never leave a session unnamed or with a stale label. Do not ask — just rename.
